export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <p className="animate-pulse font-mono text-sm uppercase tracking-[0.3em] text-mbg-ink/60">
        Memuat data nasional…
      </p>
    </main>
  );
}
